# Git 提交规则

## 格式

```
<类型>: <描述>
```

**类型**：`feat` | `fix` | `docs` | `refactor` | `chore`

## 原则

- 描述简洁，一行讲清变更内容
- 一次提交只做一件事
- 含多个子项时，首行写整体任务，后续行缩进对齐，用 `-` 列出子项：

  ```
  feat: 完成用户模块开发
  - 新增注册/登录功能
  - 实现密码加密存储
  - 添加邮箱验证逻辑
  ```

## 推送

- 单人开发：提交后直接 `git push origin master`
- 推送前确保无残留调试代码